---
title: MIoT Spec 云端错误码
source: https://iot.mi.com/v2/new/doc/help-support/faq/spec/error-code
captured_at: 2026-06-16
tags: [miot, spec, error-code, troubleshooting, 设备控制, 错误码]
category: miot
summary: MIoT Spec 控制云端返回的错误码及故障排查指南，覆盖扩展程序、云对云、小爱语控等场景
---

# MIoT Spec 云端错误码

通过扩展程序、云对云等方式控制设备时，米家云端可能会返回错误码，开发者可根据错误码排查问题。

扩展程序、云对云或小爱控制设备时出现错误码（`-70xxxyzzz` 格式），开发者可按照下方内容排查问题。

> 原文更新时间：2025/09/26

## 错误码列表

### -705001000

**说明**：开放平台服务器内部错误

**故障解决**：

**可能的原因：**
- 请检查 Spec 操作时参数是否正确。
- 请检查是否是非白名单用户测试白名单固件。
- 请检查当前用户是否有权使用设备的固件版本。

### -706012000

**说明**：三方云服务器内部错误

**故障解决**：

**云对云特有原因：**请检查三方云的回应是否正常。
- 比如 siid 和 piid 等数据应该为数字，不能是字符串。
- type 应该填写 Spec urn，而不能是 model 等信息。

### -705004000

**说明**：MIoT Spec 服务器内部错误

**故障解决**：

**可能的原因：**
- 请检查 Spec 操作时参数是否正确。
- 请检查是否是非白名单用户测试白名单固件。
- 请检查当前用户是否有权使用设备的固件版本。

### -705005000

**说明**：订阅 ID(subscriptionId) 编码错误

### -705006000

**说明**：notify error

### -704002000

**说明**：设备错误（通用）
三方云请求参数不正确

**故障解决**：

- 此错误一般是设备对扩展程序的 Spec 操作返回了错误码导致。请自行在[控制台 - 运营 - 通信日志](https://iot.mi.com/fe-op/operationCenter/log/device#tab=COMMUNICATION)查看设备的原始错误码。
**可能的原因：**
- 扩展程序操作 Spec 太频繁导致设备/网关没法处理进而报错。
- 设备/网关网络不好或设备/网关自身原因导致未及时回复扩展程序的 Spec 请求。如果是需要网关的设备，且回复的 `description` 字段为 `get rpc invalid code:-9`。请尝试重启网关后再试。
- 如果是获取属性值，网关没有当前属性的缓存值。需要扩展程序等待网关获取到缓存值后再试。
- 操作了功能定义中没有的属性或者方法导致设备报错。
- 设置的属性值/执行方法携带的属性值无法被设备处理导致设备报错。
**BLE/BLE Mesh 设备特有的原因：**
- 设备没有 properties_changed 上报，扩展程序就去获取属性导致网关报错。具体原理请[参考米家 BLE Mesh 支持的功能](embedded-dev/ble-mesh/mesh_basic#%E6%B6%88%E6%81%AF%E4%B8%8B%E8%A1%8C%20-get_properties)。
- 固件侧的模板没有跟平台功能定义侧选择的一样导致网关无法正常处理。请将固件侧的模板正确修改后，重新绑定设备。
- 修改功能定义后立即开始测试 Spec 功能。修改功能定义后，请等待 30 分钟后重启网关再进行测试。
**小爱语控特有的原因：**当前控制依赖的智能场景被用户删除导致无法控制。
**云对云接入特有原因：**
- 请求参数不正确。
- 云应用没有权限。

### -702000000

**说明**：OK

### -702010000

**说明**：accept

### -704010000

**说明**：未认证

**故障解决**：

**BLE 设备特有原因：**
- 扩展程序获取属性时，设备还未通过 `properties_changed` 上报属性值。请扩展程序等待设备上报属性值后重试，或排查设备属性上报流程是否正确。
- 设备已经上报过属性值，但是由于老网关上报规则的限制，导致上报不成功。请使用新网关进行测试，或等待第二天再进行测试。
- BLE 设备不支持通过网关下发 RPC 指令。
- 可能是扩展程序通过 `getPropertiesValue(params, 2)` 接口获取属性值，将第二个参数设置成 `2`。即仅通过向设备下发 RPC 指令获取属性值。请将第二个参数设置成 `3` 或不设置第二个参数。
- 或者通过网关对 BLE 设备进行了执行方法操作。BLE 设备不支持通过网关发送下行指令。请通过蓝牙直连操作，不要通过网关执行方法。
**灯组特有原因：**当前灯（或者子灯组）在灯组内，不能由小爱进行控制。设计上，小爱只能控制最上层的灯组。
**网关子设备特有原因：**将子设备共享给其他用户，但是没有共享网关，导致其他用户无法控制设备。请将网关也共享给其他用户。
**云对云/独立 App 特有原因：**
- 应用未注册，无权限控制。
- 应用刚获取审批，需要时间才能同步。请等待半小时后再试。
- 请求参数错误。
**小爱语控特有原因：**特定的老设备不支持被小爱语控控制
**可能的原因：**
- 设备已经被解绑，无法执行 Spec 指令。请检查设备绑定状态。
- 子状态码为：`-40121111` 时请[提交工单](https://iot.mi.com/fe-op/personalCenter/feedback)检查设备后台配置是否被标记为 `不支持 Spec`

### -704000000

**说明**：错误的请求

**故障解决**：

**云对云/独立 App 特有原因：**
- 应用未注册，无权限控制。
- 应用刚获取审批，需要时间才能同步。请等待半小时后再试。
- 请求参数错误。

### -704000001

**说明**：错误的请求体

**故障解决**：

**可能的原因：**请求参数或请求体错误。

### -704001000

**说明**：定时只支持小米设备

### -702022036

**说明**：操作正在处理中

**故障解决**：

**可能的原因：**
- 当前写入的属性值或者执行方法正在操作中。可能是操作太频繁导致上一次写入操作还在进行中。
- 获取设备列表时间太长。可能是用户的设备太多，请稍候再试。
**云对云特有原因：**上一次的写入操作，三方云服务超时未回应。

### -704042010

**说明**：- 触发场景异常。
- 获取设备列表失败。

### -704042009

**说明**：未找到场景

### -704042012

**说明**：场景无权限

### -704090001

**说明**：未找到设备

**故障解决**：

**可能的原因：**
- 白名单权限问题，请规范测试流程。
- 非白名单用户使用白名单固件，请使用白名单账户进行测试。
- 将用户添加到白名单让用户绑定设备后，再次将用户白名单权限移除。
- 白名单用户将设备共享给非白名单用户使用。
- 用户刚被加入到白名单，添加完白名单后请等待半小时后再试。
**云对云特有原因：**三方云告知米家设备不存在。

### -704042001

**说明**：未找到设备

### -704042011

**说明**：设备离线

**故障解决**：

**可能的原因：**设备已经离线，请检查设备网络。

### -704012901

**说明**：token 不存在或过期

### -704012902

**说明**：token 非法

### -704012903

**说明**：授权过期

### -704012904

**说明**：设备未授权控制能力给小爱

**故障解决**：

**可能的原因：**
- 白名单权限问题，非白名单用户测试白名单产品导致小爱无法控制设备。
- 设备未授权控制能力给小爱。请在「米家 App - 我的 - 语音控制 - 语音控制授权管理」中打开控制权限。
- 操作的设备已经被解绑。

### -704012905

**说明**：设备未绑定

**故障解决**：

**可能的原因：**设备已经被解绑，无法执行 Spec 指令。请检查设备绑定状态。

### -704012906

**说明**：认证失败

### -704040002

**说明**：服务不存在

**故障解决**：

**可能的原因：**
- 服务(Service)不存在，请对照功能定义检查 `siid` 是否存在。
- 如果功能定义有多个版本，请检查当前固件关联的功能定义版本是否包含操作的服务。
- 如果操作的服务刚添加，请等待半小时后再操作。

### -706010002

**说明**：远程服务异常

**故障解决**：

**可能的原因：**设备对于控制指令不能正确处理。请在[控制台 - 运营 - 通信日志](https://iot.mi.com/fe-op/operationCenter/log/device#tab=COMMUNICATION)查询设备对于此控制返回的状态码。

### -706010004

**说明**：MIoT 错误

### -706010005

**说明**：属性缓存失败

### -704040003

**说明**：属性不存在

**故障解决**：

**可能的原因：**
- 属性(Property)不存在，请对照功能定义检查 `siid` 和 `piid` 是否存在。
- 如果功能定义有多个版本，请检查当前固件关联的功能定义版本是否包含操作的服务。
- 如果操作的属性刚添加，请等待半小时后再操作。

### -704030013

**说明**：属性不可读

**故障解决**：

**可能的原因：**
- 功能定义中设置了该属性(Property)没有读(read)权限，请对照功能定义检查属性权限。
- 设备告知读取属性失败。设备在 RPC 指令的回复中，回复了错误码 `-4001`。请在[控制台 - 运营 - 通信日志](https://iot.mi.com/fe-op/operationCenter/log/device#tab=COMMUNICATION)查询设备通信日志并结合固件日志排查问题。

### -704030023

**说明**：属性不可写

**故障解决**：

**可能的原因：**
- 功能定义中设置了该属性(Property)没有写(write)权限，请对照功能定义检查属性权限。
- 设备告知写属性失败。设备在 RPC 指令的回复中，回复了错误码 `-4002`。请在[控制台 - 运营 - 通信日志](https://iot.mi.com/fe-op/operationCenter/log/device#tab=COMMUNICATION)查询设备通信日志并结合固件日志排查问题。

### -704030033

**说明**：属性不可上报

**故障解决**：

**可能的原因：**功能定义中设置了该属性(Property)没有上报(notify)权限，请对照功能定义检查属性权限。

### -704030992

**说明**：请求过于频繁，本次请求被拒绝

**故障解决**：

**可能的原因：**请求太频繁，请降低请求频率。

### -704220043

**说明**：属性值不正确

**故障解决**：

**可能的原因：**
- 扩展程序或三方云 `set_properties` 时，传递的 `value` 值不符合功能定义的要求。比如功能定义设置该属性格式为 `int32`，但是传入的是 `string`。
- 设备或三方云 `get_properties` 的返回结果，传递的 `value` 值不符合功能定义的要求。比如功能定义设置该属性格式为 `int32`，但是传入的是 `string`。请自行获取固件日志排查。
- 设备对于控制指令没有响应，导致模组向云端回复 `user ack timeout`（如果是 Android OT 或 Linux OT，会向云端回复 `busy`）。请结合固件日志排查固件业务逻辑问题。
- 设备对于控制指令不能正确处理。请在[控制台 - 运营 - 通信日志](https://iot.mi.com/fe-op/operationCenter/log/device#tab=COMMUNICATION)查询设备对于此控制返回的状态码并结合固件日志排查问题。
**灯组特有原因：**暂时无法控制未上线设备组成的灯组，请查看[帮助中心](https://iot.mi.com/fe-op/personalCenter/info?faq=2455-1)进行解决。

### -705201013

**说明**：读属性失败

### -706012013

**说明**：读属性失败

**故障解决**：

**云对云特有原因：**
- 三方云对 `set_properties` 请求回复了错误码导致设置失败。
- 无法从三方云获取到属性值，请检查三方云返回数据是否正确。

### -706012014

**说明**：读属性失败

### -705201023

**说明**：写属性失败

### -706012023

**说明**：写属性失败

**故障解决**：

**云对云特有原因：**无法从三方云返回结果中获取到设定的属性值，请检查三方云返回数据是否正确。
**红外设备特有原因：**不支持的红外码库，请[提交工单](https://iot.mi.com/fe-op/personalCenter/feedback)进行人工排查更新。

### -705201033

**说明**：上报属性失败

### -706012033

**说明**：上报属性失败

### -706012043

**说明**：订阅失败

### -704040004

**说明**：事件不存在

**故障解决**：

**可能的原因：**
- 事件(Event)不存在，请对照功能定义检查 `siid` 和 `eiid` 是否存在。
- 如果功能定义有多个版本，请检查当前固件关联的功能定义版本是否包含操作的事件。
- 如果操作的事件刚添加，请等待半小时后再操作。

### -704222034

**说明**：事件参数数量不匹配

**故障解决**：

**可能的原因：**
- 设备上报事件( `event_ocured` )时，携带的参数数量和功能定义中的数量不一致。
- 如果功能定义有多个版本，请检查当前固件关联的功能定义版本中的事件参数数量。
- 如果操作的事件刚修改，请等待半小时后再操作。

### -704040005

**说明**：方法不存在

**故障解决**：

**可能的原因：**
- 方法(Action)不存在，请对照功能定义检查 `siid` 和 `aiid` 是否存在。
- 如果功能定义有多个版本，请检查当前固件关联的功能定义版本是否包含操作的方法。
- 如果操作的方法刚添加，请等待半小时后再操作。

### -705201015

**说明**：方法执行失败

### -706012015

**说明**：方法执行失败

**故障解决**：

**可能的原因：**设备告知方法执行失败。设备在 RPC 指令的回复中，回复了非 `0` code，请在[控制台 - 运营 - 通信日志](https://iot.mi.com/fe-op/operationCenter/log/device#tab=COMMUNICATION)查询设备通信日志并结合固件日志排查问题。

### -704220035

**说明**：方法输入参数错误

**故障解决**：

**可能的原因：**
- 方法输入参数值错误。请检查功能定义中输入参数值的格式要求。比如功能定义设置某个属性格式为 `int32`，但是传入的是 `string`。
- 设备告知方法输入参数错误。设备在 RPC 指令的回复中，回复了错误码 `-4006`。请在[控制台 - 运营 - 通信日志](https://iot.mi.com/fe-op/operationCenter/log/device#tab=COMMUNICATION)查询设备通信日志并结合固件日志排查问题。

### -704220025

**说明**：方法输入参数数量不匹配

**故障解决**：

**可能的原因：**
- 扩展程序或三方云 `doAction` / `action` 时，输入的参数数量和功能定义中的输入参数数量不匹配。请对比功能定义检查参数中 `in` 数组的内容。
- 如果功能定义有多个版本，请检查当前固件关联的功能定义版本中的方法输入参数数量。
- 如果操作的方法刚修改，请等待半小时后再操作。
- 设备执行 `action` 返回的输出参数数量错误。请在[控制台 - 运营 - 通信日志](https://iot.mi.com/fe-op/operationCenter/log/device#tab=COMMUNICATION)查询设备通信日志检查 out 数组长度是否和功能定义中的数量是否一致。
**BLE Mesh 设备特有原因：**BLE Mesh 设备通过网关执行方法时，最多只支持一个参数。请修改功能定义或使用蓝牙直连执行此方法。

### -704222035

**说明**：方法输出参数数量不匹配或参数错误

**故障解决**：

**可能的原因：**
- 设备对于 `action` 操作的回复中，输出的参数数量和功能定义中的输出参数数量不匹配或参数值错误。请对比功能定义检查参数中 `out` 数组的内容。
- 如果功能定义有多个版本，请检查当前固件关联的功能定义版本中的方法输出参数数量以及参数值格式。
- 如果操作的方法刚修改，请等待半小时后再操作。

### -704044006

**说明**：未找到功能定义

**故障解决**：

**可能的原因：**
- 此设备还未初始化功能定义，请初始化后等待半小时后再试。
- 功能定义刚初始化，请等待半小时后再试。

### -705204006

**说明**：非法的功能定义

**故障解决**：

**可能的原因：**功能定义中，某些属性的范围超过了属性格式的范围。比如属性格式为 `int32`，其数值范围最大值不能大于 `2147483648`，最小值不能小于 `-2147483647`。

### -705204007

**说明**：实例未加载

### -704041007

**说明**：cloud not found

### -704220008

**说明**：非法的 ID(SIID、PIID、EIID、AIID)

**故障解决**：

**可能的原因：**
- 设备告知属性/方法/事件不存在。设备在 RPC 指令的回复中，回复了错误码 `-4003`。请在[控制台 - 运营 - 通信日志](https://iot.mi.com/fe-op/operationCenter/log/device#tab=COMMUNICATION)查询设备通信日志并结合固件日志排查问题。
- 服务、属性、事件或方法不存在。请对照功能定义检查 `siid`、`piid`、`eiid` 以及 `aiid` 是否存在。
- 如果功能定义有多个版本，请检查当前固件关联的功能定义版本是否包含操作的服务。
- 如果操作的服务、属性、事件或方法刚添加，请等待半小时后再操作。
- 设备当前固件版本没有在开发者平台开启测试。

### -704220009

**说明**：非法的 uid

### -704220010

**说明**：非法的订阅 ID(subscriptionId)

### -704053100

**说明**：无法执行此操作

**故障解决**：

**可能的原因：**设备告知执行操作失败。设备在 RPC 指令的回复中，回复了非 `0` code。请在[控制台 - 运营 - 通信日志](https://iot.mi.com/fe-op/operationCenter/log/device#tab=COMMUNICATION)查询设备通信日志并结合固件日志排查问题。

### -704083036

**说明**：操作超时

**故障解决**：

**可能的原因：**
- 设备告知操作超时。设备在 RPC 指令的回复中，回复了错误码 `-3`。请在[控制台 - 运营 - 通信日志](https://iot.mi.com/fe-op/operationCenter/log/device#tab=COMMUNICATION)查询设备通信日志并结合固件日志排查问题。
- 设备固件对于 Spec 请求没进行回复，导致模组/OT 自动回复 `-3` 超时，请结合固件日志排查问题。

### -704040999

**说明**：功能未上线

### -704013101

**说明**：红外设备不支持此操作

### -704053101

**说明**：摄像机休眠中
